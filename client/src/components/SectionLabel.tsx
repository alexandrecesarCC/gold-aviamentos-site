export function SectionLabel({ children }: { children: React.ReactNode }) {
  return (
    <div className="text-sm font-semibold text-[#C9A227] tracking-widest uppercase mb-4">
      {children}
    </div>
  );
}
